export type EzyStoryModel = {
  pictureUrl: string,
  streamLogoUrl: string;
  streamName: string;
  headline: string;
  description: string;
};

export type EzyStoriesResultModel = {
  result: any[]
};
